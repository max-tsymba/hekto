import React, { useContext } from 'react';
import Context from '../../public/index';

const Navbar = () => {
    const {user} = useContext(Context);
    
    return (
        <div>
            
        </div>
    )
}

export default Navbar
